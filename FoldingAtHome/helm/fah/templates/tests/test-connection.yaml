apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fah.fullname" . }}-test-connection"
  labels:
{{ include "fah.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fah.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
